---
// Temporary placeholder for build
export interface Props {
  title?: string
  [key: string]: any
}

const { title = 'Placeholder' } = Astro.props
---

<div>
  <h1>{title}</h1>
  <p>
    This is a placeholder component used during build to avoid null byte issues.
  </p>
  <slot />
</div>
