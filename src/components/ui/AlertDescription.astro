---
import type { HTMLAttributes } from 'astro/types'
import { cn } from '../../lib/utils'

type Props = HTMLAttributes<'div'>

const { class: className, ...attrs } = Astro.props
---

<div class={cn('text-sm mt-1', className)} {...attrs}>
  <slot />
</div>
