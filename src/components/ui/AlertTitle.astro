---
import type { HTMLAttributes } from 'astro/types';
import { cn } from '../../lib/utils';

type Props = HTMLAttributes<'h3'>;

const { class: className, ...attrs } = Astro.props;
---

<h3 class={cn('text-sm font-medium', className)} {...attrs}>
  <slot />
</h3>
