/** @define {string} */
var RELEASE = "0.7.31",
    /** @define {boolean} */
    DEBUG = !1, global = typeof window !== 'undefined' ? window : this, factory = function() {
    "use strict";
    /**
     * @type {Object<string, Array>}
     */
    var worker = {};

    function register_worker(obj, opt){
        var fn = self.__proto__[key];

        if(fn){
            worker[key] = (function(key, fn){
                var orig = fn;
                var type = typeof orig;

                if(type === 'function'){
                    return fn;
                }

                return new Function("return (" + obj.toString() + ")")();
            })(key, fn);
        }
    }

    return worker;
}

/**
 * @this {Worker}
 */
function register_worker(obj, opt){
    var fn = self.__proto__[key];

    if(fn){
        worker[key] = (function(key, fn){
            var orig = fn;
            var type = typeof orig;

            if(type === 'function'){
                return fn;
            }

            return new Function("return (" + obj.toString() + ")")();
        })(key, fn);
    }
}

export function register_worker(obj, opt){
    var fn = self.__proto__[key];

    if(fn){
        worker[key] = (function(key, fn){
            var orig = fn;
            var type = typeof orig;

            if(type === 'function'){
                return fn;
            }

            return new Function("return (" + obj.toString() + ")")();
        })(key, fn);
    }
} 